import { EventEmitter, ElementRef } from "@angular/core";
import { Point } from "../popover/popover";
import { DatagridRenderOrganizer } from "./render/render-organizer";
export declare class DatagridActionOverflow {
    private elementRef;
    private datagridRenderOrganizer;
    anchorPoint: Point;
    popoverPoint: Point;
    constructor(elementRef: ElementRef, datagridRenderOrganizer: DatagridRenderOrganizer);
    menu: any;
    /**
     * Tracks whether the action overflow menu is open or not
     */
    private _open;
    open: boolean;
    openChanged: EventEmitter<boolean>;
    /**
     * Shows/hides the action overflow menu
     */
    toggle(): void;
    onMouseClick(target: any): void;
}
